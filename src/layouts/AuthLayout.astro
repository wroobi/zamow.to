---
import Layout from "./Layout.astro";

interface Props {
  title?: string;
}

const { title = "Autentykacja" } = Astro.props as Props;
---

<html lang="pl">
  <head>
    <meta charset="utf-8" />
    <title>{title}</title>
    <meta name="description" content="Logowanie i zarządzanie kontem w zamow.to" />

    <Layout title={title}>
      <main class="mx-auto max-w-3xl px-4 py-8">
        <div class="mx-auto w-full max-w-md rounded-xl border bg-white dark:bg-neutral-900 shadow-sm p-6">
          <div class="flex items-center gap-2 mb-6 justify-center">
            <div class="h-8 w-8 rounded-full bg-neutral-200 dark:bg-neutral-800" aria-hidden="true"></div>
            <span class="font-semibold tracking-tight">zamow.to</span>
          </div>
          <slot />
        </div>
      </main>
    </Layout>
  </head>
</html>
